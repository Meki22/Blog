<html lang="en">
    <%- include ('./partials/head.ejs')%>
<body>
<%- include ('./partials/nav.ejs')%>
  <div>
    <h1>All Blogs</h1>
    <% if(blogs.length > 0) { %>
        <%blogs.forEach(blogs=> { %>
            <h3><%= blogs.title%></h3>
            <p><%= blogs.snippet%></p>
            <% }) %>
            <% }else { %>
                <p>no blogs </p>
                <%}%>

      </div>
</body>
<%- include('./partials/footer.ejs')%>
</html>